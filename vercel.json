{
  "builds": [
      {
        "src": "./index.js",
        "use": "@vercel/node"
      }
    ],
  "routes": [
    { "src": "/(.*)", "dest": "/" },
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}
